
// 1) git init:

// It is used to initialise a git repo
// It creates a hidden folder name git in the directory

// 2) git add [.] or [filename]:

// It is used to add files before commiting them. 
// [.] is used to add all file in the directory or you can specify a filename to add

// 3) git commit -m"message":

// It is used to commit files in the git
// a brief message is added for that commit

// 4) git branch [name]:

// It is used to create a new branch of the repo

// 5) git checkout
